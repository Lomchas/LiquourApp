@{
    ViewData["Title"] = "Verificación de Edad";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-warning text-dark">
                <h2 class="text-center">@ViewData["Title"]</h2>
            </div>
            <div class="card-body">
                @if (ViewData["Message"] != null)
                {
                    <div class="alert alert-danger">
                        @ViewData["Message"]
                    </div>
                }
                <div class="text-center mb-4">
                    <p class="lead">Para acceder a LiquourApp, debes ser mayor de 18 años.</p>
                    <p>Por favor, ingresa tu fecha de nacimiento para verificar tu edad.</p>
                </div>
                <form asp-action="VerifyAge" method="post">
                    <div class="form-group mb-3">
                        <label for="dateOfBirth" class="control-label">Fecha de nacimiento</label>
                        <input id="dateOfBirth" name="dateOfBirth" class="form-control" type="date" required />
                    </div>
                    <div class="form-group text-center">
                        <button type="submit" class="btn btn-primary">Verificar edad</button>
                    </div>
                </form>
            </div>
            <div class="card-footer">
                <div class="alert alert-info mb-0">
                    <small>Al verificar tu edad, confirmas que la información proporcionada es correcta y aceptas nuestros términos y condiciones.</small>
                </div>
            </div>
        </div>
    </div>
</div>